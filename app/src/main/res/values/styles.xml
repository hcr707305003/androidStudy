<resources>
    <!-- Base application theme. -->
    <style name="replaceflag_AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/replaceflag_colorprimary</item>
        <item name="colorPrimaryDark">@color/replaceflag_colorprimarydark</item>
        <item name="colorAccent">@color/replaceflag_coloraccent</item>
    </style>

    <style name="replaceflag_AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="replaceflag_TextAllCapsFalseButton">
        <item name="android:background">@drawable/replaceflag_round_corner_button</item>
    </style>

    <style name="replaceflag_AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="replaceflag_AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="replaceflag_dfhdfhdfghjsdfgdfbrte" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RoundCornerButtonStyle">
        <item name="android:textSize">@dimen/FontSizeBig</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/round_corner_button</item>
        <item name="android:layout_margin">8.0dip</item>
    </style>
</resources>
